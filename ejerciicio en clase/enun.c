#include <stdio.h>




/*declaracion de enum*/
/*tipo identificador{lista_de_indentificadores};*/
/*TAREA PARA EL LUNES IMPRIMIR VAR1, VAR2,VAR3*/


enum DIAS{L, M, MI, J, v, D};
enum MESES{EN=1, FE, MAR, AB, MAY, JUN, JUL, AG, SE, OC, NO, DI};
enum FRUTA{ MANZANA, MANGO, DURAZNO, PERA, UVA, PINA};


enum PRECIOAUTO{NUEVO= 10, SEMINUEVO= -5, USADO, INSERVIBLE};
int main(void)
enum {PRO, MATEDIS, MATII, TGS,ELEC}VAR1,VAR2,VAR3;
{
printf("%d", USADO);
for (enum MESES = EN;index <= DI;index++)
{

printf("%d", index);
switch(index)
{
case EN:
printf("enero");
break;
case OC;
printf ("ocubre");
break;
}
}
return 0;

}
